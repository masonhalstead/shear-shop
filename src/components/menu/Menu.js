import React from 'react';
import PropTypes from 'prop-types';
import cn from './Menu.module.scss';

export const Menu = () => {
    return <div className={cn.menuContainer}>
        <p></p>
    </div>
}